# abrooks.github.io

Notes to self:

https://chatgpt.com/c/68fd06a4-26c8-8321-88d0-e554179d3a00

## Usage 

```bash
# Build and serve locally
docker compose up --build
```

Serving at [localhost:4000](http://localhost:4000)

```bash
# clean build
docker compose run --rm jekyll bundle exec jekyll build
# update gems
docker compose run --rm jekyll bundle update
```

